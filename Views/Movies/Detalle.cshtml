@model Pelicula



@* <div class="text-center">
    <h1 class="display-5">Detalle</h1>
</div> *@


<div>
<table class="table table-dark table-striped">
  <thead class="text-left">
    <tr>
      <th scope="col">@Model.Nombre</th>
    </tr>
  </thead>
  <tbody>
        <tr>
            <td>Puesto: <button class="btn btn-success">@Model.Código</button></td>
        </tr>
        <tr>
            <td>Dirigida por: @Model.Direccion</td>
        </tr>    
        <tr>
            <td>Género: @Model.Género</td>
        </tr>
        <tr>
            <td>@Model.Duración minutos.</td>
        </tr>
        <tr>
            <td>@Model.Puntuación /10 ☆.</td>
        </tr>
</tbody>
</table>
    <div class="text-center">
        <button class="btn btn-primary">Editar</button>
        <button class="btn btn-danger">Eliminar</button>
    </div>

</div>


@* <div class="container text-center">
  <div class="row">
    <div class="col">
      <h3 class="display-5">@Model.Nombre</h3>
    </div>
  </div>
  <div class="row">
    <div class="col">
      1 of 3
    </div>
    <div class="col">
      2 of 3
    </div>
  </div>
</div> *@








@* <div class="text-left">
    <h3>@Model.Nombre</h3>
    <p>Duración: @Model.Duración minutos.</p>
</div> *@


@* <div class= "text-left">
<ol>
    @foreach (var peli in Model)
    {
        <li>@peli.Nombre, Dureación: @peli.Duración minutos</li>
    }
</ol>
</div>  *@


@* @{
    var nombre = Model.Nombre;
    var codigo = Model.Código;
    var direccion = Model.Direccion;
    var genero = Model.Género;
    var duracion = Model.Duración;
    var puntuacion = Model.Puntuación;
}



<table class="table table-dark table-striped">
  <thead>
    <tr>
      <th scope="col">@nombre</th>
    </tr>
  </thead>
  <tbody>
         <tr>
            <td>Puesto: @codigo</td>
            <td>Dirigida por: @direccion</td>
            <td>Género: @genero</td>
            <td>@duracion minutos.</td>
            <td>@puntuacion /10 estrellas.</td>
          </tr>
</tbody>
</table> *@